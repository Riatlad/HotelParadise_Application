package com.HotelParadise.App.Model;

/*
import org.junit.Test;
import static org.junit.Assert.assertEquals;

public class HabitacionTest {

    @Test
    public void testHabitacion() {
        Habitacion habitacion = new Habitacion("01", "Estandar", "Disponible", "Solo");

        assertEquals("01", habitacion.getIdHab());
        assertEquals("Estandar", habitacion.getTipo());
        assertEquals("Disponible", habitacion.getEstado());
        assertEquals("2", habitacion.getCapacidad());
    }

} */
